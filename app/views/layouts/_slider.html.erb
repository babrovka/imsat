<div class="span12">
<div id="slider" class="slider">
	<ul>
		<% Slide.order("sn ASC").each do |slide| %>
		<li>
			<%= image_tag(slide.logo, :size => "940x339") %>
			
			<div class="s-text">
				<span class="st-title"><%= slide.title %></span>
				<p><%= slide.note %></p>
				<%= link_to "далее...", slide.link %>
			</div>
		</li>
		<% end %>
	</ul>
		
	<div id="paging" class="slider-paging"></div>
</div>
</div>